import { ComingSoon } from "@/components/layout/ComingSoon";
import { Bot } from "lucide-react";

export default function AgentsPage() {
  return (
    <ComingSoon
      icon={Bot}
      title="Agents"
      description="Monitor running and historical AI agent runs — scope, analysis, planning, and code generation stages. This view is under construction."
    />
  );
}
