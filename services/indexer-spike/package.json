{
  "name": "@atlas/indexer-spike",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase-0 ingestion spike (docs/04, PROJECT_HANDOFF §8 task 6). Runnable with plain Node 22+ (native TS type-stripping), zero external services. Production indexer is Rust; this validates the pipeline shape and the secret-scan-before-embed invariant.",
  "scripts": {
    "start": "node src/index.ts",
    "test": "node test/run.ts"
  },
  "dependencies": {
    "@qdrant/js-client-rest": "^1.18.0",
    "glob": "^13.0.6",
    "tree-sitter": "^0.21.1",
    "tree-sitter-javascript": "^0.21.2",
    "tree-sitter-python": "^0.21.0",
    "tree-sitter-typescript": "^0.21.2"
  }
}
